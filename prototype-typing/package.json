{
  "name": "prototype-typing-strategy",
  "private": true,
  "description": "THROWAWAY PROTOTYPE — answers wayfinder ticket #6 (does plugin-inferred typing survive the factory?). Do not ship.",
  "type": "module",
  "scripts": {
    "typecheck": "tsc --noEmit",
    "declaration": "tsc --noEmit false --declaration --emitDeclarationOnly --outDir .dts-probe"
  },
  "dependencies": {
    "better-auth": "1.6.23",
    "effect": "4.0.0-beta.93"
  },
  "devDependencies": {
    "typescript": "^5.9.0"
  }
}
